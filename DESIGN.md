# 私人生活记录系统 UI 2.0

> 2026-09-03 实施补充：用户已批准产品审计并启动 UI 品质提升。视觉实现以本文末尾第 23、24 节为准；最新动效请求及第 25 节替代此前的动效参数。已经按阶段覆盖首页与四类回顾页面。

> 设计交付版本：2026-09-02。依据用户确认的 Editorial Journal 明暗首页参考图整理。
> 本文件是视觉与交互交接规范，不是开发实施授权。本轮不修改生产代码、数据库、路由、原始内容或 PRODUCT.md。

## 1. Design Philosophy

这是一个随时可以回来写一句话的私人地方，不是一项需要完成的任务。

用户往往带着尚未整理好的念头打开首页。设计应当容纳片段、停顿和长短不一的表达，不要求用户先决定类型、标题、分类或完整性。

主路径：打开首页 → 点击「写点什么」→ 写文字，可选图片与地点 → 保存到本地 → 离开。

以文字、空白和时间建立秩序。日期让用户知道自己身处何时；原文承担主要情感；照片是原文的一部分；界面不代替用户解释生活。

- DESIGN_VARIANCE：6。桌面日期侧栏与正文错位形成辨识度；手机严格单列，不制造阅读障碍。
- MOTION_INTENSITY：3。只做用户操作后的轻反馈，不自动播放、不滚动编排。
- VISUAL_DENSITY：3。留白足够，但输入和保存不因过度留白被推出首屏。
- 基础语言是自定义 editorial 产品 UI，不宣称使用某个官方设计系统。
- Product Design 负责任务层级和交互一致性；taste-skill 仅用于视觉判断，不套用营销页 hero、必须放图片等规则。

## 2. Scope 与需求优先级

PRODUCT.md 是产品需求最高来源；本次交付限定于当前已完成的核心记录与回顾界面。PRODUCT.md 中更大 V1 范围不是本轮新增 UI 的授权。

| 页面族 | 保持现有 URL | 本次效果图主状态 |
| --- | --- | --- |
| 首页 | `/` | 快速记录展开、最近随笔、追加 |
| 日记 | `/diary`、`/diary/new`、`/diary/[id]` | 编辑器；列表、阅读规则见下文 |
| 时间线 | `/timeline` | 按真实创建时间排列的随笔与日记 |
| 日历 | `/calendar` | 月历和选中日期详情 |
| 搜索 | `/search` | 普通关键词搜索结果 |

不加入 Tags、Favorites、Trash、AI、Daily Summary、地图、健康、云同步、社交或分享。没有打卡、连续天数、数量排行榜、完成率、情绪评分或提醒。

保留现有业务不变量：Moment originalText 不可编辑；Append 是独立的后续补充；Diary 标题可选、正文必填且可编辑；本地保存不等待定位或网络；已有确认放弃和错误恢复不能消失。

### 参考图的解释边界

参考图确认的是排版气质与明暗主题，不是逐像素业务说明。

- 参考图中的「0字」不进入实现：当前没有字数计数需求。
- 手机图不得作为省略随笔正文的依据。不能把原文第一句拆成系统生成的标题，也不能裁掉剩余正文。
- 保留「取消」「添加具体地点」「添加图片」「追加」等现有能力，即使参考图漏画。
- 展开写作状态是效果图展示重点，不等于首页加载时自动聚焦、自动弹键盘或自动请求定位。
- 「正在获取位置」是临时状态，不能变成固定装饰文案。
- 参考图外框和投影是展示画板，不是产品卡片。实际页面铺满视口，无外层阴影容器。

## 3. Visual Language

日页式构图、宋体标题、安静的自然底色、单一朱红主操作。明暗是同一个空间在不同光线下的状态，不是两套设计。

信息优先级：原文与输入内容 > 主要操作 > 日期分组与日记标题 > 时间地点 > 辅助操作。

不使用渐变、毛玻璃、发光、装饰网格、巨型圆角、背景插画或夸张的空状态图。没有品牌口号、欢迎语和人格化系统旁白。

## 4. Color Tokens

颜色按语义使用；正文和 metadata 不通过降低整个容器 opacity 实现层级。

| Token | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `surface` | `#FAF9F6` | `#1C1D1C` | 页面和无边框写作区 |
| `surface-subtle` | `#F0F1EC` | `#272925` | hover、轻微选中背景 |
| `text-primary` | `#252621` | `#E7E5DF` | 原文、日记正文、主要标题 |
| `text-secondary` | `#666960` | `#AAA9A3` | 日期时间、地点、说明、placeholder |
| `line-subtle` | `#DADCD5` | `#3A3D39` | 非必要装饰分隔线 |
| `control-boundary` | `#85897F` | `#85897F` | 必须可辨认的输入边界 |
| `action-fill` | `#A72F26` | `#B34E43` | 保存、搜索等当前主操作 |
| `action-text` | `#FFFFFF` | `#FFFFFF` | 实心主按钮文字 |
| `focus-ring` | `#A72F26` | `#E58E80` | 键盘焦点 |
| `error-text` | `#A72F26` | `#E58E80` | 错误文案，必须同时有文字解释 |
| `selection-fill` | `#A72F26` | `#B34E43` | 日历选中日期 |

已按 sRGB 相对亮度公式计算：

| 组合 | Light | Dark |
| --- | --- | --- |
| 正文 / 页面 | 14.48:1 | 13.42:1 |
| 次要文字 / 页面 | 5.31:1 | 7.18:1 |
| 白字 / 主按钮 | 6.82:1 | 5.14:1 |
| 必要控件边界 / 页面 | 3.39:1 | 4.74:1 |

这是 token 对比度检查，不是完整 WCAG 认证。细线不能独自承担必要状态。不要直接从生成图采样色值代替此表。

### Dark Mode

首次跟随系统 `prefers-color-scheme`。本轮不增加设置页或主题开关入口。演示图同时呈现两种主题，但产品同一页面不混用明暗区块。

深色背景不用纯黑，正文不用大面积刺眼纯白。照片保持原色、原始亮度，不反相、不加全局滤镜、不写回原图。切换主题不改变布局、换行规则、控件尺寸、图片顺序或滚动位置。

## 5. Typography

字体以中文实际阅读为先。展示字体最多两个家族，不以英文字体替代中文设计。

- Editorial：思源宋体 / Noto Serif CJK SC，系统回退为 Songti SC、SimSun、serif。用于日期、页标题、日记标题和记录阅读文本。
- UI：系统中文无衬线，PingFang SC、Microsoft YaHei、system-ui、sans-serif。用于按钮、标签、输入编辑状态和 metadata。
- 中英数字保持同一行基线；时间可以用 tabular numerals，不把所有日期变成终端风格。
- 若未来自托管字体，保留原许可，优先按字重和字符子集减少体积。加载失败不能阻止输入；避免字体切换引起明显位移。

| 角色 | Desktop | Mobile | 行高 / 字重 |
| --- | --- | --- | --- |
| 页标题 / 主日期 | 40px | 30px | 1.25 / 400 |
| 写作邀请 | 30px | 26px | 1.4 / 400 |
| 分组标题 | 21px | 20px | 1.5 / 500 |
| 日记标题 | 32px | 27px | 1.4 / 500 |
| 随笔正文 | 17px | 16px | 1.75 / 400 |
| 日记正文 | 18px | 17px | 1.85 / 400 |
| 输入正文 | 17px | 16px | 1.75 / 400 |
| Append 正文 | 16px | 16px | 1.75 / 400 |
| metadata | 13px | 13px | 1.55 / 400 |
| 按钮 / 链接 | 14px | 15px | 1.4 / 500 |

中文正文每行建议约 28-36 字，手机约 20-23 字；英文长文不超过约 65 字符。保留原始换行，不强制每条原文只有三行。不要通过极细字重和超浅颜色营造高级感。

## 6. Spacing / Radius / Borders / Shadows

- 间距基础：4、8、12、16、24、32、48、64、80px。
- 标签到控件：8px；正文到 metadata：8px；照片到正文：12-16px；Append 到原记录：16px。
- 同日记录之间：28-32px；跨日期分组：48-56px；首页写作与最近记录：40-48px。
- 小控件和照片 radius：4px；正文分组 radius：0。无胶囊按钮和大圆角卡片。
- 必要控件边界 1px；焦点 2px、offset 3px；其他分隔线 1px。
- 无常规 box-shadow。依靠间距、字号和细线表达层级。
- 长列表只在确实需要断开阅读时使用单条分隔线，不给每行同时画上下边框。

## 7. Desktop / Mobile / Responsive Rules

### Desktop，1024px 及以上

主壳最大宽度 1120px，左右至少 40px。1440px 视口左右通常各约 160px。

内部采用日期/标题侧区 192px + 间隔 48px + 正文区 minmax(0, 1fr)。正文区最大 760px。用户文字实际行宽仍受 28-36 个中文字符约束，不把所有正文铺满屏宽。

首页侧区放日期；回顾页面侧区放页标题/日期锚点；不是可展开的功能侧栏。照片只在正文仍有充足宽度时并排，避免窄文字栏。

### Tablet，768-1023px

左右留白 32px，日期移至正文上方。读写列最大 720px。不要保留空白侧栏挤压正文。

### Mobile，320-767px，以 390px 验收

390px 左右各 24px，正文宽 342px；320px 左右各 16px。顶部 28-32px，底部 32px + 安全区域。只有单列。

- 日期、写作入口、编辑区、最近记录按自然文档流排列。
- 触控区域至少 44×44px，按钮间至少 8px。视觉可以很轻，点击区域不能缩小。
- 不自动弹键盘；用户点击写作入口后才聚焦。键盘弹出后保存动作不能被固定栏挡住，内容可自然滚动。
- 默认不增加常驻底部导航或浮动新建按钮。更多入口允许自然换行，不能挤成一排微小链接。
- 多图在窄屏正文之后排列。不将文字与图片强行左右并排；保留原始顺序。
- 同一 Moment 正文不能因为移动端而消失或被标题替代。首屏放不下就自然滚动，不缩小字号塞满。
- 200% 文字缩放无重叠；320px 无页面级横向溢出。

## 8. Navigation

首页层级严格为写点什么 → 最近记录 → 更多。首页底部弱导航保留现有顺序：时间线、日历、日记、搜索。

二级页保留各自现有导航链接与名称，不增设全局工作区导航。返回首页、返回日记等是轻量文字链接；hover 使用下划线，focus 使用明确外框。

不为「更多」新增路由。它是已有次级入口区域的标签，不强制增加一层弹窗。不能为了统一外观增加原本不存在的操作。

## 9. Buttons 与 Inputs

### Buttons

主按钮：朱红背景，白字，44px 最小高度，左右 padding 20px，4px radius。普通状态一个明确主意图；不要让添加图片和保存同等醒目。

次按钮：文字或细边框，surface 底色。取消低于保存。追加使用持续可见的轻文字按钮，不只在 hover 时出现。

Hover 使用既有颜色的轻微变化，不发光；Pressed 轻微压下反馈即可；Disabled 用可解释的状态，不仅降低整个区域透明度。Saving 标签为「保存中…」，防止重复提交。

### Inputs

快速记录编辑区与页面同色，安静底线界定输入区域。可见字段标签始终保留，不能只依赖 placeholder。焦点必须明显，不能为了无边框风格取消 keyboard focus。

快速记录最小编辑高度建议 144px，随文字增长。日记编辑区使用更长阅读空间；搜索输入最小高度 44px。手机输入字号至少 16px。

错误紧邻对应字段或保存动作，保留全部输入与已选图片。不要清空表单后提示重试。

## 10. Home / 快速记录

### 默认状态

显示今天日期和「写点什么」可点击入口。入口像打开一页纸而非营销按钮；不要显示不可操作的假 textarea。下方立即是已有最近 Moments；保留已有最多 20 条的读取边界，不擅自改变分页逻辑。

### 写作状态

点击入口后展开原位编辑器并聚焦正文。顺序为正文、可选城市与具体地点、可选图片与预览、取消/保存。桌面辅助操作和保存可以同一行，手机允许拆成两行。

不等待定位；定位失败或无权限时安静回退，具体地点仍可手动填写。不展示经纬度、网络错误堆栈或需要用户处理的定位任务。

选择图片后展示已有预览与移除操作。保存成功后才结束输入状态并更新最近列表。保存失败保留内容。取消有未保存内容时保留既有确认机制。

不加入默认自动保存、快捷键新行为、AI 润色或字数目标。

## 11. Moment

原文是唯一主体，允许一句或多段。无头像、点赞、转发、评论数量、卡片标题。

桌面：时间窄列 + 原文宽列；地点置于正文下方，不塞入狭窄时间列。照片可在充分宽度下右侧排列，否则落在原文后方。

手机：时间与日期小行 → 完整原文 → 地点 → 图片 → 后续追加 → 追加操作。顺序变化只为阅读，不改变时间或内容。

不可编辑 originalText，也不出现看似可编辑的铅笔、光标或 hover 编辑菜单。不对原文做 AI 重写、自动摘要、补标题或未经授权的标点替换。

## 12. MomentAppend

表达「后来补充」，不是「有人回复」。原文后下移 16px，缩进 16px，细竖线 1px，时间与正文分开。

每条 Append 保留自己的完整时间，跨天时不能只显示时分。正文 16px，不靠过小字号弱化。按既有创建时间升序排列，不作为时间线的独立根记录。

追加入口用「追加」。进入后显示「追加文字」标签、输入区、取消与保存追加。不能给原记录增加可编辑能力；本轮不增加追加的编辑、删除或图片功能。

## 13. Images

图片是原始记录的一部分，不是装饰素材。效果图照片仅为演示，不写入用户数据。

- 一图：自然比例，宽度受正文约束；避免默认占满整个首屏。
- 多图：尊重已有附件顺序，2-3 列规则排列；单元格约 96-160px，窄屏优先 2 列。数量更多时自然增加行数。
- 如保留现有方形缩略图，可用 cover，但不能裁切或改写原始 Blob。没有新建图库、灯箱、下载或分享功能的授权。
- 深色主题不处理图片色相和亮度。圆角 4px，无投影、标签覆盖和相框。
- 图片加载失败仅影响该图片区域，原文和追加继续可读。加载前预留比例，避免滚动跳动。
- 保留已有可用的文件名替代文本；不得虚构 AI 图像描述。

## 14. Diary

与 Moment 的区别不是额外工具栏，而是进入一个愿意认真写下来的长文空间。

### 列表 `/diary`

页标题「日记」，一个「写日记」主入口。每项使用日期、可选标题和既有正文预览，不套卡。无标题时直接以正文开头呈现，不生成并保存标题。无一天一篇限制。

### 新建 `/diary/new` 与编辑状态

保留「返回日记」、可选标题、必填正文、取消、保存日记。标题字号大于正文；正文是一页舒展的文字，不是 CMS 表单。

编辑器仍是正常可访问的文本输入，不暗示 Markdown block、富文本、模板、格式工具栏、字数目标、图片或地点支持。长正文行宽约 34 个中文字符，段间以原始换行呈现。

桌面保存动作紧邻文章末尾；手机同样位于文档流，不用挡住键盘的底部固定栏。保留未保存离开保护、重复提交保护和错误恢复。

### 阅读 `/diary/[id]`

阅读与编辑使用相同字宽和段落节奏，防止切换后文字大幅跳动。阅读状态移除表单边界，保留已有编辑入口；不展示存储 ID。创建时间不因编辑而改变。不能把编辑时间当作新的生活事件插入 Timeline。

## 15. Timeline

「我的生活沿时间发生」，不做社交 Feed。日期锚点是识别元素，竖线不是必需的装饰。

按既有真实 createdAt 和稳定 ID 顺序展示，最新在前。日期分组之间留白 48-56px；组内记录使用相同 Moment 阅读对象。

随笔以原文呈现；日记使用小型「日记」类型文字、可选标题和既有预览链接，不套醒目彩色卡。无标题日记显示正文预览。

Append 留在所属 Moment 下方，不按追加日期挪动整条记录。图片读取失败不掩盖其他内容。保留显式「加载更多」，不改成无限自动加载，也不增加类型筛选。

桌面日期锚点在左侧，手机上移为组标题。文字与图片不挤入过窄的时间轨道。

## 16. Calendar

月历表达「这一天有内容」，不表达「这一天完成任务」。只保留上一月、下一月、返回当前月与现有日记录类型筛选。

周一开头；按设备本地时间显示日界。当前稿锚点为 2026-09-02 星期三。2026 年 9 月 1 日位于星期二，9 月 30 日位于星期三。

| 状态 | 视觉 | 语义 |
| --- | --- | --- |
| 普通日期 | 正常数字，无填色 | 仍可选择 |
| 今天 | 数字外 1px 轮廓环 | aria-label 含「今天」 |
| 有记录 | 数字下一个小的中性色点 | aria-label 含「有记录」 |
| 已选中 | 朱红小圆角底 + 白字 | aria-pressed=true |
| 今天且选中 | 实心选中底，外围保留独立轮廓 | 两种状态不互相抹掉 |
| 键盘焦点 | 额外 2px focus ring | 不等于选中 |

有记录的点具有真实语义，不是装饰 status dot。无热力渐变、记录数量、连续线、漏记状态、完成标记。不要把无记录日期禁用。

月份读取中不将所有日期误呈现为「没有记录」；提示正在读取状态，失败与空数据分开。

桌面月历与日详情可并排，月历约 340px，详情至少 420px；空间不足时上下排列。390px 时日历 7 列，每格有效触控宽高至少 44px。

未选日期：轻提示「选择一个日期查看内容。」选中后保留日期标题、全部/随笔/日记三个现有筛选。切换月份、清除日选择等行为保持现有逻辑。详情复用 Timeline，无独立第二套卡片。

## 17. Search

输入 → 找到过去。显示「搜索」页标题、明确「关键词」标签、输入和搜索按钮。保留 Enter 提交，不擅自改为输入即实时搜索。

未输入不展示热词、搜索历史、AI 推荐或建议问题。关键词改变后遵循现有结果清除和重新提交机制。

结果按既有根记录创建时间排序；Moment 的命中 Append 留在父 Moment 内，不产生重复根结果。保留已有「命中：原文 / 追加内容 / 标题 / 正文」来源标识，弱于原文。

不新增长期索引、模糊匹配、语义搜索或手动标签过滤。结果展示沿用 Timeline 行宽、metadata、照片、追加规则。保留「加载更多」与结束状态。不显示未经当前查询提供的总条数。

零结果：「没有找到相关记录。」读取失败：「搜索暂时无法完成。」两者不能混淆。未输入提示：「输入关键词后查看结果。」

## 18. Interaction States 与 Motion

| 场景 | 必须保留的状态与反馈 |
| --- | --- |
| 首页写作 | 默认入口、展开、已填、图片预览、具体地点展开、保存中、失败、成功退出 |
| Append | 阅读、展开输入、验证错误、保存中、失败保留、成功追加 |
| Diary | 列表空态、创建、阅读、编辑、未保存离开确认、保存中、错误 |
| Timeline | 首次读取、无内容、局部图片/追加失败、加载更多、全部读取结束 |
| Calendar | 月读取中、月读取失败、今天/记录/选中组合、日读取中、无该类记录 |
| Search | 初始、输入、提交中、结果、零结果、失败、加载更多、结果结束 |

Loading 只在需要时出现，不用持续闪动骨架屏。错误文案不抢过原文，不强迫离开当前输入。状态消息用合适的 live region；不要每次键入都朗读。

Motion：颜色与透明度 120-180ms；展开/页面状态切换最多 180ms。默认不做首屏入场、列表逐条飞入、视差、滚动劫持、图片悬浮或循环动画。展开不硬编码高度以致裁正文。尊重 reduced-motion，直接切到最终状态。

## 19. Accessibility

- 普通文字最少 4.5:1；必要控件边界和状态最少 3:1。最终应针对所有 hover、focus、selected、error 状态再验收。
- 所有可操作对象支持键盘；Tab 顺序与视觉/文档顺序一致。焦点不能因保存、翻页或重排无理由丢失。
- 用真实 button、link、label、input 和 textarea 语义。每页一个清晰 h1；组标题层级连续。
- 日期具有可理解的完整名称；日历不只靠红色区分状态。不宣称已有 ARIA grid 键盘模式，除非完整实现相应规范；现阶段保留按钮模式即可。
- 放弃输入的确认保留可访问行为；不凭视觉稿替换成不可操作的自制弹层。
- 长文字、长地点、无标题日记、多图、加载失败均需测试。英文长单词和 URL 可以断行但不能覆盖邻列。
- 主题变化、字体加载、图片加载不应造成明显布局抖动。
- 不能仅凭生成图宣称读屏、键盘、真实软键盘或 WCAG 全面合规。

## 20. Do / Don't

| Do | Don't |
| --- | --- |
| 原文决定页面节奏 | 为了好看截掉移动端原文 |
| 同一阅读对象跨页面复用 | 首页、搜索、日历各自发明卡片 |
| 以留白、文字和细线组织 | 用阴影和大圆角包住一切 |
| 可见但低调的入口 | 所有操作都藏在 hover 下 |
| 有记录的日历轻提示 | 记录数量、连续天数、完成度 |
| 日期是真实信息 | 城市天气时间的装饰标语 |
| 深色保留照片原色 | 反相或永久修改原图片 |
| 保存先确认本地成功 | 等定位、AI 或网络后才结束 |
| Moment 不可编辑，只追加 | 借 UI 重设计修改原始数据 |

## 21. 交付文件与效果图

五页规范已完成；五张新效果图尚未完成。内置 Image Gen 连续返回网络错误，没有成功的新图。时间线未完成的请求已停止；日历与搜索未继续发送。本轮没有交付可点击原型，也没有用旧首页参考图冒充新页面效果图。

计划每张展示同一页面的 Desktop Light、Mobile Light、Desktop Dark、Mobile Dark。日记主图展示编辑器，其列表与阅读状态按本规范实现，不新增页面路由。以下文件名仅为后续生成计划，目前没有对应文件。

| 文件 | 页面 |
| --- | --- |
| `01-home.png`（待生成） | 首页 |
| `02-diary.png`（待生成） | 日记编辑 |
| `03-timeline.png`（待生成） | 时间线 |
| `04-calendar.png`（待生成） | 日历 |
| `05-search.png`（待生成） | 搜索 |

用户确认的两张来源图保存在 [明色参考](design/ui-v2/reference-light.png) 与 [深色参考](design/ui-v2/reference-dark.png)。它们是之前生成并由用户提供的设计参考，不是本轮新图。五页完整提示词见 [PROMPTS.md](design/ui-v2/PROMPTS.md)。所有示例文字、照片、时间只用于设计演示。

### 开发交接优先级

1. PRODUCT.md 与数据不变量。
2. 本轮明确功能边界和现有路由/行为。
3. 本 DESIGN.md 的精确尺寸、tokens、状态规则。
4. 用户确认的明暗参考图的视觉语言。
5. 新生成效果图的细节；若字形、日期、按钮、比例与以上冲突，以规范为准并指出差异。

不能直接把效果图铺成页面背景或照抄图中可能的中文错误。实际页面必须由可访问的真实控件与用户数据构成。

## 22. 验证与未解决事项

本次是设计文档与静态效果图交付，不做工程完成声明。未运行生产 typecheck、lint、unit/integration/E2E、build；正式实现时须按 AGENTS.md 完整运行，任何失败不得声称开发完成。

本轮验证：颜色对比度计算、五页功能范围核对、两张来源图人工查看、来源图片存在性、交付链接与文件完整性。五张新效果图因生成失败未验证；实际 390px DOM、软键盘、读屏、字号缩放、长文与大数据量也未在新 UI 上验证。

后续技术注意点：中文自托管字体体积与回退布局；原图存储和缩略图裁剪分离；长记录不截断的性能；日历焦点与日期组合状态；深色首帧主题一致性。它们是落地验收项，不是本轮授权的工程改造。

现有工作区有不属于本次设计的代码与构建产物改动；本轮不覆盖、不回滚、不提交这些改动。

## 23. 2026-09-03 首页品质提升（Phase UI-1）

本节替代首页旧的居中邀请、全部 4px 圆角、禁止页面进入动画和 1.5 秒保存等待规范。产品结构、数据模型和查询边界保持不变。

- `src/app/design-system.css` 是共享语义 tokens 的唯一来源。暖白/深灰页面加轻微纸面层次；正文使用系统中文衬线回退，编辑和操作使用系统无衬线。无需网络字体才能写作。
- 间距沿用 4/8/12/16/24/32/48/64/80px；按钮 8px、输入纸面 12px、照片 6px，记录正文不套卡。阴影仅用于输入纸面的微弱层次；blur token 预留给真正需要叠层的界面，首页不使用模糊。
- 首页桌面为日期侧区和 720px 阅读列，移动端单列。整行「写点什么」可点击，左对齐文字和右侧轻量新增符号；展开后标题位置稳定。
- 原位展开由现有 Framer Motion 使用 spring（stiffness 380 / damping 38 / mass 0.8）驱动真实内容高度。页面进入为 6px 位移与淡入，360ms；普通状态过渡 140/240ms，使用 `cubic-bezier(0.32, 0.72, 0, 1)`。reduced-motion 直接完成状态切换。
- 共享 WritingTextarea 使用 `field-sizing: content`，不支持的浏览器使用按内容和宽度测量的 fallback；不裁去原文、不强加字数限制。
- 取消与保存反馈结束后把焦点交回入口；退出中的编辑区域 inert，不进入键盘导航。增加跳到正文链接和读取失败重试。
- 图片保留原始文件名辅助文本，使用占位尺寸与淡入，并在加载失败时保留可阅读反馈。图片入口使用 Radix Icons；不引入图库或图片编辑。
- 保存按钮使用同一强调色，成功反馈由 1500ms 缩短至 700ms，保持原标签、真实保存状态和防重复提交。Moment/Append 列表仍在持久化完成时立即刷新。
- 本阶段不增加顶栏、底部导航或新的首页模块。其他路由只继承共享 tokens、焦点和保存按钮，页面布局留待 UI-2 至 UI-4。

截图、对比入口和验证记录见 `design/ui-refresh/README.md`。实际 iOS 软键盘、VoiceOver 与 Safari 尚待设备验收；Chromium 中已覆盖 fallback，不等同于 Safari 实测。

## 24. UI-2 至 UI-5 实施补充

- 二级页面使用相同的返回导航、标题节奏与可访问焦点；保留各页既有链接。首页没有新增全局功能导航。
- Timeline 以日期侧栏组织连续的 Moment 与 Diary，移动端将日期上移。Append 留在所属记录中，显示完整日期和时间。
- Diary 使用宽度受控的长文空间；输入、阅读和列表具有共同字阶。空标题不生成标题；加载与空状态分开，返回链接沿用未保存确认。
- Calendar 桌面采用 340px 月历加日详情，移动端单列。320px 时日期区向页边适量延伸，保证七列 44px 目标；普通 Tab、Enter/Space 和月内方向键均可用，未宣称完整 ARIA grid 模式。记录点、今天边框、选中底色和焦点轮廓分开表达。
- Search 保持显式提交。使用与正文同序的命中高亮，保留准确的原始字符；长日记展示命中处上下文。只宣告当前已显示数量，不伪造总数。
- 旧路由全局样式已移至各自 CSS module，避免加载顺序覆盖。全站进入、日期切换、结果出现、按压和图片反馈使用同一组动效 tokens，不做列表逐条延迟或滚动编排。
- 全部页面验证 320/390/430/768/1440px、明暗主题、200% 字号缩放、无横向滚动和 44px 交互目标。最终截图来自本地生产构建，不是云端上线证明。

## 25. 统一动效系统（用户明确要求的后续实施）

本节替代先前的动效时长、曲线、列表禁止错峰和 700ms 保存反馈规则；不改变现有文案、色彩、字体、信息架构与核心布局。

- 唯一参数源为 `src/components/ui/motion.ts`。曲线 `[0.22, 1, 0.36, 1]`；瞬时/快速/普通/缓慢为 160/220/420/580ms。根服务端 layout 将同一组参数写成 CSS 变量，首次绘制不等待 hydration。
- 首页日期、写作区、最近标题和导航分别进入，8px / 420ms，间隔 80ms。最近记录为 8px / 420ms，间隔 60ms，最多累计到第六项；之后新增项无额外延迟。数据刷新不重播首页层级动画。
- 记录入口文字 hover 上移 2px，下划线展开，普通按钮按下为 0.98。链接只改变颜色和细线，不缩放。导航组件按 pathname 设置 `aria-current="page"`，不使用 pathname 作为页面 key。
- 编辑器由 AnimatePresence 控制进入/退出，位移 6px。ResizeObserver 只观察内层内容，用真实数值高度驱动 420ms、bounce=0 的阻尼 spring；不混用异步 `height: auto` 测量。快速反向开关保留单一编辑区域，退出区域 inert，焦点交回触发入口。输入变化只在实际内容尺寸变化时测量。
- 共享 StatefulButton 保留稳定的外部尺寸；三个状态按最长标签预留空间，内部文字交叉淡入/淡出并移动 4px。保存中图标连续旋转，成功对勾短描边，显示「已保存」1100ms。真实写入完成即刷新记录；文字和图片在反馈期间保留，反馈完成后再关闭编辑器。计时器有卸载和操作序号保护，失败保留输入及可访问错误。
- 最近记录和追加使用稳定实体 ID 与 position-only layout 动画，不缩放长文。未变化附件复用 Blob URL 与图片 DOM；过期查询不创建 URL，刷新失败保留最近成功视图，替换后或卸载时释放资源。
- 图片 opacity 淡入、精确鼠标 hover 最多 1.02；尺寸、裁切和圆角保持原样。没有新增灯箱。
- 其他路由仅包职责单一的客户端 PageEntrance；根 layout 仍为服务端组件。420ms / 8px 可靠进入，动画结束释放 transform；不阻塞原生路由或强制退出动画。
- `useReducedMotion` 与精确 CSS media query 取消位移、错峰、spinner 和等待式过渡，保留焦点与状态反馈。没有全局 `*` 动画覆盖、永久 will-change、逐帧 React state 或新增动画依赖。

实现、前后截图、测试与边界见 [动效验收记录](design/motion-system/README.md)。
